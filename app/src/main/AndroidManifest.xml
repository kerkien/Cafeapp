<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CafeApp"
        tools:targetApi="31">

        <!-- Landing page is now the launcher -->
        <activity
            android:name=".Landingactivity"
            android:exported="true">  <!-- ✅ Changed to true -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Loginactivity"
            android:exported="false" />

        <!-- ✅ Removed intent-filter from MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".MenuForTableActivity"
            android:exported="false" />
        <activity
            android:name=".MenuPreviewActivity"
            android:exported="false" />
        <activity
            android:name=".SetupTablesActivity"
            android:exported="false" />
        <activity
            android:name=".addmenuitem"
            android:exported="false" />
    </application>

</manifest>